# Laboratorio 8 - Area de Seguranca (Cerca Virtual)
# Testado com Python 3.12 no Windows (setembro/2026).
#
# NAO adicione "opencv-python" nesta lista. O mediapipe ja instala o
# "opencv-contrib-python", e os dois pacotes juntos quebram o "import cv2".

mediapipe>=0.10.14
numpy>=1.26
scikit-learn>=1.3
pyserial>=3.5
